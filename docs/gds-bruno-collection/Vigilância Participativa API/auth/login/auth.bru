meta {
  name: auth
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@example.com",
    "password": "123456"
  }
}

script:post-response {
  bru.setVar("token", res.getBody().token)
}
