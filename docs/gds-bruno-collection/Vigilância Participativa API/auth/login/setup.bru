meta {
  name: setup
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@example.com",
    "password": "admin123"
  }
}

script:post-response {
  bru.setVar("token", res.getBody().token)
}
