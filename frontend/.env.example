# Copie para .env.local (desenvolvimento local) ou use os ficheiros versionados:
# - .env.development → API dev (build CI em `develop`)
# - .env.production → API prod (build CI em `main`)

VITE_API_BASE_URL=http://localhost:3000/v1
